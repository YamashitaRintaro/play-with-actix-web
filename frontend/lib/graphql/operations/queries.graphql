#import "./fragments.graphql"

query Me {
  me {
    ...UserFields
  }
}

query Timeline {
  timeline {
    ...TweetFields
  }
}

query Tweet($id: UUID!) {
  tweet(id: $id) {
    ...TweetFields
  }
}

query Comments($tweetId: UUID!) {
  comments(tweetId: $tweetId) {
    ...CommentFields
  }
}
